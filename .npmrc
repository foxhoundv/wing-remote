# Wing Remote has no JavaScript build tooling and no npm dependencies.
# This file exists solely to suppress spurious warnings (e.g. inflight memory
# leak) that can appear when Docker BuildKit or Compose v2 invokes npm
# internally during the build process.
fund=false
audit=false
loglevel=error
